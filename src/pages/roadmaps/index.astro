---
import MainLayout from '../../layouts/MainLayout.astro'

const categories = ['Finanzas', 'Inversiones', 'Blockchain']
---

<MainLayout title='Sebastian Patiño Caro | Roadmaps' description='Roadmaps sobre finanzas, inversiones y blockchain. Aprende sobre educación financiera, criptomonedas y estrategias de inversión con contenido práctico y actualizado.'>
  <main class='container mx-auto px-4 py-8'>
    <!-- Filtros -->
    <!-- <section class='mb-8'>
      <div class='flex flex-wrap gap-4 items-center'>
        <h2 class='text-xl font-semibold'>Filtrar por categoría:</h2>
        <div class='flex flex-wrap gap-2'>
          {
            categories.map((category) => (
              <button class='px-4 py-2 bg-white rounded-full shadow-sm hover:bg-gray-100 transition-colors'>
                {category}
              </button>
            ))
          }
        </div>
      </div>
    </section> -->

    <!-- Buscador -->
    <!-- <section class='mb-8'>
      <div class='max-w-xl'>
        <input
          type='search'
          placeholder='Buscar rutas...'
          class='w-full px-4 py-2 rounded-lg border border-gray-300 focus:outline-none focus:ring-2 focus:ring-blue-500'
        />
      </div>
    </section> -->

    <!-- Lista de Rutas -->
    <section class='grid grid-cols-1 md:grid-cols-2 gap-8'>
      <div class="col-span-full text-center py-12">
        <h3 class="text-2xl font-semibold text-gray-600">Próximamente</h3>
        <p class="mt-2 text-gray-500">Estaré trabajando en nuevas rutas de aprendizaje para ti.</p>
      </div>
    </section>
  </main>
</MainLayout>

